package de.keksuccino.fancymenu.util.rendering.ui.window;

public class WindowOverlay {

    //TODO build window overlay (to display and handle windows) + window system
    //TODO build window overlay (to display and handle windows) + window system
    //TODO build window overlay (to display and handle windows) + window system
    //TODO build window overlay (to display and handle windows) + window system
    //TODO build window overlay (to display and handle windows) + window system

}

/**
 * Runtime support used by FancyMenu's client mixins.
 *
 * <p>The {@code de.keksuccino.fancymenu.mixin.mixins.common} prefix is reserved by the Mixin configuration.
 * These classes must remain outside that prefix so Mixin does not reject them as direct class loads. Types and
 * members called by mixin bytecode must also remain public because that bytecode executes from transformed
 * {@code net.minecraft} classes.</p>
 */
package de.keksuccino.fancymenu.mixin.support.client;

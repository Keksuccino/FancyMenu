
plugins {
    id 'java'
    id 'idea'
    id 'maven-publish'
    id 'fabric-loom' version '1.10-SNAPSHOT'
}

base {
    archivesName = "${mod_id}"
}

repositories {
    flatDir { dirs 'libs' }
}

dependencies {

    minecraft "com.mojang:minecraft:${minecraft_version}"
    mappings loom.layered() {
       officialMojangMappings()
       parchment("org.parchmentmc.data:parchment-1.21.8:2025.07.20@zip")
    }
    modImplementation "net.fabricmc:fabric-loader:${fabric_loader_version}"
    modImplementation "net.fabricmc.fabric-api:fabric-api:${fabric_version}"
    implementation group: 'com.google.code.findbugs', name: 'jsr305', version: '3.0.1'
    implementation project(":common")
    include(implementation(annotationProcessor("io.github.llamalad7:mixinextras-fabric:0.4.1")))
    include(implementation 'com.github.Keksuccino:japng:0.5.3')
    include(implementation 'com.github.rtyley:animated-gif-lib-for-java:animated-gif-lib-1.7')
<<<<<<< HEAD
    modImplementation "maven.modrinth:melody:1.0.12-1.21.6-fabric"
    modImplementation "maven.modrinth:konkrete:1.9.12-1.21.6-fabric"
    modImplementation "maven.modrinth:modmenu:15.0.0-beta.2"
    modCompileOnly 'com.cinemamod:mcef:2.1.6-1.21.1'
=======
    modImplementation "maven.modrinth:melody:1.0.10-1.21-fabric"
    modImplementation "maven.modrinth:konkrete:1.9.9-1.21.4-fabric"
    modImplementation "maven.modrinth:modmenu:13.0.0-beta.1"
    modImplementation "it.crystalnest:fancy-entity-renderer-fabric:$minecraft_version-$fer_version"
    modCompileOnly 'com.cinemamod:mcef:2.1.6-1.21.4'
>>>>>>> e58d878... v3.7.0

}

loom {
    if (project(":common").file("src/main/resources/${mod_id}.accesswidener").exists()) {
        accessWidenerPath.set(project(":common").file("src/main/resources/${mod_id}.accesswidener"))
    }
    mixin {
        defaultRefmapName.set("${mod_id}.refmap.json")
    }
    runs {
        client {
            client()
            setConfigName("Fabric Client")
            ideConfigGenerated(true)
            runDir("run_client")
        }
        server {
            server()
            setConfigName("Fabric Server")
            ideConfigGenerated(true)
            runDir("run_server")
        }
    }
}

tasks.withType(JavaCompile).configureEach {
    source(project(":common").sourceSets.main.allSource)
}
tasks.withType(Javadoc).configureEach {
    source(project(":common").sourceSets.main.allJava)
}
tasks.named("sourcesJar", Jar) {
    from(project(":common").sourceSets.main.allSource)
}

processResources {
    from project(":common").sourceSets.main.resources
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifactId base.archivesName.get()
            from components.java
        }
    }
    repositories {
        maven {
            url "file://" + System.getenv("local_maven")
        }
    }
}
